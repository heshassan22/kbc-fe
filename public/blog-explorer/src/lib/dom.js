// Small DOM helpers — TODO

// Create an element with an optional class and text content.
export const el = (tag, className, text) => {
  // TODO: create the element; set className + textContent when provided; return it
};

// Shorten long text for a card excerpt.
export const excerpt = (text, max = 100) => {
  // TODO: return `text` trimmed to ~`max` chars, with an "…" when it was longer
};
