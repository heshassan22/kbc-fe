// Pure data helpers — TODO

// Attach each post's author name from the users list.
// Return a NEW array of { ...post, author } — do not mutate the inputs.
export const joinAuthors = (posts, users) => {
  // TODO: build a userId -> name lookup, then map posts to include `author`
};
